import type {Metadata} from "next";
import {ChapterFour} from "../chapter-four";
export const metadata:Metadata={title:"Define and organize routes — PHPAML Tutorial",description:"Build PHPAML web and API routes with methods, parameters, constraints, names, groups, middleware, and tests."};
export default function Page(){return <ChapterFour locale="en"/>}
