import Link from "next/link";
import {CodeBlock,Footer,Header} from "../components";
import "./chapter-one.css";import "./chapter-four.css";import "./chapter-eight.css";
type L={n:string;fr:string;en:string;fp:string[];ep:string[];frule:string;erule:string};
const lessons:L[]=[
{n:"08.1",fr:"Pourquoi une base de données ?",en:"Why a database?",fp:["Jusqu’ici, l’application sait recevoir une tâche et afficher une interface, mais une donnée conservée seulement dans une variable disparaît à la fin de la requête. Une base apporte une mémoire durable partagée par toutes les requêtes.","Une base relationnelle organise les informations dans des tables, lignes et colonnes. Elle peut imposer des types, des valeurs obligatoires, des identifiants uniques et des relations que le code seul ne garantit pas toujours.","SQLite stocke toute la base dans un fichier et ne demande aucun serveur séparé. Cela en fait un excellent choix pour apprendre, tester, développer localement et déployer de petites applications."],ep:["So far, the application can receive a task and display an interface, but data kept only in a variable disappears after the request. A database provides durable memory shared by all requests.","A relational database organizes information into tables, rows, and columns. It can enforce types, required values, unique identifiers, and relationships that code alone may not guarantee.","SQLite stores the entire database in one file and requires no separate server. It is excellent for learning, testing, local development, and small deployments."],frule:"La base garantit la persistance et une partie essentielle de l’intégrité des données.",erule:"The database guarantees persistence and an essential part of data integrity."},
{n:"08.2",fr:"Configurer avec l’environnement",en:"Configure through the environment",fp:["Le code doit savoir quel pilote utiliser et où trouver la base, mais ces valeurs changent entre ordinateur, tests et production. Elles appartiennent donc à l’environnement plutôt qu’aux classes PHP.","Le fichier .env local peut définir DB_CONNECTION=sqlite et DB_DATABASE=runtime/database/app.sqlite. phpaml.json décrit les choix non secrets du projet, tandis que les secrets ne doivent jamais être commités.","Utilisez aml env:init pour créer l’environnement à partir d’un exemple documenté. Une nouvelle installation doit pouvoir comprendre les variables attendues sans recevoir les valeurs privées de votre machine."],ep:["Code needs a driver and database location, but those values differ across local, test, and production environments. They belong to configuration rather than PHP classes.","A local .env can define DB_CONNECTION=sqlite and DB_DATABASE=runtime/database/app.sqlite. phpaml.json describes non-secret project choices, while secrets must never be committed.","Use aml env:init to create the environment from a documented example. A new installation should understand expected variables without receiving private values from your machine."],frule:"Le dépôt documente les clés; chaque environnement possède ses propres valeurs.",erule:"The repository documents keys; every environment owns its values."},
{n:"08.3",fr:"Choisir le chemin du fichier SQLite",en:"Choose the SQLite file path",fp:["Le fichier SQLite contient de vraies données et évolue pendant l’exécution. Il ne doit pas vivre dans public, où un visiteur pourrait le télécharger, ni dans un dossier de code versionné.","PHPAML place les données d’exécution sous runtime/database. Le serveur Web doit pouvoir écrire dans ce dossier, mais public/index.php reste le seul point d’entrée accessible depuis Internet.","Utilisez un chemin absolu résolu depuis la racine du projet pour éviter qu’un changement de répertoire courant crée accidentellement une seconde base vide."],ep:["The SQLite file contains real data and changes at runtime. It must not live in public, where visitors could download it, or in a versioned source folder.","PHPAML places runtime data under runtime/database. The Web process needs write permission there, while public/index.php remains the only Internet entry point.","Use an absolute path resolved from the project root so a changed working directory cannot accidentally create a second empty database."],frule:"Une base SQLite est une donnée privée d’exécution, jamais une ressource publique.",erule:"A SQLite database is private runtime data, never a public asset."},
{n:"08.4",fr:"Comprendre PDO et la connexion",en:"Understand PDO and connection",fp:["PDO fournit une interface commune aux bases SQL. Une connexion SQLite utilise un DSN commençant par sqlite:, puis le chemin du fichier. Le framework centralise cette création afin que les modèles ne reconstruisent pas la connexion.","Activez le mode d’erreur par exceptions et les contraintes de clés étrangères. Une erreur silencieuse produit des données incohérentes; une exception claire peut être journalisée et transformée en réponse appropriée.","Préparez toujours les requêtes contenant des valeurs externes. Les paramètres liés séparent le code SQL des données et empêchent une saisie utilisateur de modifier la structure de la requête."],ep:["PDO provides one interface for SQL databases. A SQLite connection uses a DSN beginning with sqlite:, followed by the file path. The framework centralizes creation so models do not rebuild connections.","Enable exception error mode and foreign-key constraints. Silent failures create inconsistent data; clear exceptions can be logged and translated into appropriate responses.","Always prepare queries containing external values. Bound parameters separate SQL code from data and prevent user input from changing query structure."],frule:"La connexion est centralisée; les valeurs externes passent par des paramètres préparés.",erule:"Connection is centralized; external values use prepared parameters."},
{n:"08.5",fr:"Concevoir la table tasks",en:"Design the tasks table",fp:["Avant d’écrire une migration, décrivez ce que représente une tâche. Elle possède un identifiant, un titre, une description facultative, un état de complétion et des dates de création et modification.","Choisissez les contraintes selon le métier : title ne peut pas être null, completed reçoit une valeur par défaut et id est la clé primaire. Une contrainte en base protège aussi les écritures provenant d’un script ou d’une future API.","N’ajoutez pas toutes les colonnes imaginables. Un schéma minimal et explicite évolue mieux qu’une table remplie de champs hypothétiques et rarement compris."],ep:["Before writing a migration, describe what a task represents. It has an identifier, title, optional description, completion state, and creation and update timestamps.","Choose constraints from business meaning: title cannot be null, completed gets a default, and id is the primary key. Database constraints also protect writes from scripts or future APIs.","Do not add every imaginable column. A minimal explicit schema evolves better than a table full of speculative fields."],frule:"Le schéma traduit les invariants durables du métier en contraintes de données.",erule:"Schema translates durable business invariants into data constraints."},
{n:"08.6",fr:"Créer une migration",en:"Create a migration",fp:["Une migration est une modification de schéma versionnée et reproductible. Elle décrit comment avancer avec up() et, lorsque cela est sûr, comment revenir avec down().", "Le nom create_tasks_table indique l’intention. Le numéro ou horodatage ordonne les migrations afin que toutes les installations appliquent exactement la même histoire.","Une migration doit rester déterministe : elle ne dépend pas de données présentes seulement sur votre ordinateur et ne demande pas une décision interactive pendant le déploiement."],ep:["A migration is a versioned, reproducible schema change. It describes how to advance with up() and, when safe, how to return with down().","The create_tasks_table name communicates intent. A sequence or timestamp orders migrations so every installation applies the same history.","A migration must remain deterministic: it cannot depend on data found only on your machine or ask interactive questions during deployment."],frule:"Le schéma appartient à l’histoire versionnée du projet, pas à une manipulation manuelle.",erule:"Schema belongs to the project's versioned history, not manual manipulation."},
{n:"08.7",fr:"Exécuter et vérifier les migrations",en:"Run and verify migrations",fp:["aml migrate applique uniquement les migrations absentes et enregistre leur exécution dans une table interne. Relancer la commande ne doit pas recréer les tables déjà présentes.","Après l’exécution, vérifiez le statut, la structure réelle de tasks et les contraintes importantes. Une commande qui termine sans erreur ne prouve pas à elle seule que le schéma correspond à votre intention.","Exécutez les migrations dans la CI sur une base neuve. Cela détecte les dépendances cachées à une base locale déjà modifiée manuellement."],ep:["aml migrate applies only missing migrations and records them in an internal table. Running it again must not recreate existing tables.","After running, inspect status, the actual tasks structure, and important constraints. A command ending without error alone does not prove schema matches intent.","Run migrations in CI against a fresh database. This catches hidden dependency on a locally hand-edited database."],frule:"Une migration est fiable lorsqu’elle construit la bonne base depuis zéro de manière répétable.",erule:"A migration is reliable when it repeatedly builds the correct database from zero."},
{n:"08.8",fr:"Annuler sans perdre les données",en:"Roll back without losing data",fp:["Le rollback exécute down() sur le dernier lot. Supprimer une table annule bien sa structure, mais détruit aussi ses lignes : une opération techniquement réversible peut donc rester dangereuse.","En développement, rollback aide à corriger une migration récente. En production, préférez souvent une nouvelle migration corrective, sauvegardez avant l’opération et évaluez l’impact sur les versions de code encore actives.","Certaines transformations de données nécessitent plusieurs étapes : ajouter une colonne nullable, remplir les valeurs, puis renforcer la contrainte dans une migration ultérieure."],ep:["Rollback runs down() for the latest batch. Dropping a table reverses structure but destroys rows, so a technically reversible operation can remain dangerous.","In development, rollback helps correct a recent migration. In production, prefer a corrective migration, back up first, and consider code versions still running.","Some data transformations need several stages: add a nullable column, populate values, then strengthen the constraint in a later migration."],frule:"Avant un rollback, distinguez réversibilité du schéma et récupération réelle des données.",erule:"Before rollback, distinguish schema reversibility from actual data recovery."},
{n:"08.9",fr:"Séparer développement, test et production",en:"Separate development, test, and production",fp:["Chaque environnement doit posséder sa propre base. Les tests ne doivent jamais toucher les tâches du développeur, et une commande locale ne doit jamais pouvoir effacer la production.","Utilisez une base temporaire ou dédiée aux tests, recréée automatiquement. Les données de démonstration proviennent de seeders ou fixtures explicites plutôt que d’une copie incontrôlée de production.","En production, limitez les permissions, sauvegardez le fichier SQLite et empêchez plusieurs déploiements concurrents d’exécuter les mêmes migrations sans coordination."],ep:["Every environment needs its own database. Tests must never touch developer tasks, and a local command must never erase production.","Use a temporary or dedicated test database recreated automatically. Demo data comes from explicit seeders or fixtures rather than uncontrolled production copies.","In production, limit permissions, back up SQLite, and prevent concurrent deployments from running the same migrations without coordination."],frule:"Les environnements partagent le schéma, jamais leurs données ni leurs secrets.",erule:"Environments share schema, never data or secrets."},
{n:"08.10",fr:"Sauvegarder et diagnostiquer",en:"Back up and diagnose",fp:["Une base dans un fichier simplifie la sauvegarde, mais copier un fichier pendant une écriture peut être risqué. Utilisez les mécanismes SQLite appropriés ou arrêtez brièvement les écritures pour obtenir un instantané cohérent.","En cas d’erreur, vérifiez le chemin réellement résolu, l’existence du dossier, les permissions, l’extension PDO SQLite et le statut des migrations. Une base vide inattendue indique souvent un mauvais chemin plutôt qu’une perte magique.","Surveillez la taille, l’espace disque et la fréquence des sauvegardes. Testez aussi la restauration : une sauvegarde non restaurable n’est qu’un sentiment de sécurité."],ep:["A file database simplifies backup, but copying during a write can be unsafe. Use appropriate SQLite mechanisms or briefly stop writes for a consistent snapshot.","When errors occur, inspect the resolved path, folder existence, permissions, PDO SQLite extension, and migration status. An unexpected empty database often means a wrong path rather than magical data loss.","Monitor size, disk space, and backup frequency. Test restoration too: a backup that cannot restore is only a feeling of safety."],frule:"La persistance est complète seulement lorsque sauvegarde, diagnostic et restauration sont prévus.",erule:"Persistence is complete only when backup, diagnosis, and restoration are planned."}
];
const envCode=`DB_CONNECTION=sqlite
DB_DATABASE=runtime/database/app.sqlite`;
const migrationCode=`final class CreateTasksTable extends Migration
{
    public function up(): void
    {
        Schema::create('tasks', function (Blueprint $table): void {
            $table->id();
            $table->string('title', 120);
            $table->text('description')->nullable();
            $table->boolean('completed')->default(false);
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('tasks');
    }
}`;
function Opening({fr}:{fr:boolean}){return <section className="lesson-rich-block"><h2>{fr?"Une application qui oublie tout après chaque requête n’est pas encore terminée.":"An application that forgets everything after each request is not finished."}</h2><p>{fr?"Les contrôleurs et les vues donnent déjà l’impression que l’application fonctionne. Pourtant, sans stockage durable, aucune tâche ne survivra au prochain démarrage. Nous allons donner une mémoire réelle au projet avec SQLite et une histoire de schéma reproductible grâce aux migrations.":"Controllers and views already make the application feel functional. Yet without durable storage, no task survives the next restart. We will give the project real memory with SQLite and reproducible schema history through migrations."}</p><p>{fr?"Le but n’est pas seulement de créer un fichier .sqlite. Vous apprendrez où il doit vivre, comment le configurer sans exposer de secret, comment construire la table tasks sur toute machine et comment faire évoluer cette structure sans improviser directement en production.":"The goal is not merely creating a .sqlite file. You will learn where it belongs, how to configure it without exposing secrets, how to build tasks on every machine, and how to evolve structure without improvising in production."}</p><div className="lesson-callout"><strong>{fr?"Projet du chapitre":"Chapter project"}</strong><p>{fr?"Configurez une base SQLite privée, créez la migration tasks, vérifiez migrate et rollback, puis préparez des bases distinctes pour développement, tests et production.":"Configure a private SQLite database, create the tasks migration, verify migrate and rollback, then prepare separate development, test, and production databases."}</p></div></section>}
function Summary({fr}:{fr:boolean}){return <section className="lesson-rich-block course-summary"><p className="kicker">{fr?"En résumé":"In summary"}</p><h2>{fr?"La base est maintenant durable, privée et reproductible.":"The database is now durable, private, and reproducible."}</h2><p>{fr?"Vous avez séparé la configuration du code, placé SQLite dans runtime, conçu un schéma minimal et transformé ce schéma en migrations versionnées. Vous savez aussi pourquoi rollback, sauvegarde et séparation des environnements exigent de la prudence.":"You separated configuration from code, placed SQLite under runtime, designed a minimal schema, and turned that schema into versioned migrations. You also know why rollback, backup, and environment separation require care."}</p><ul><li>{fr?"gardez la base hors de public et du dépôt":"keep the database outside public and source control"}</li><li>{fr?"décrivez la configuration dans .env et phpaml.json":"describe configuration through .env and phpaml.json"}</li><li>{fr?"versionnez chaque changement de schéma":"version every schema change"}</li><li>{fr?"testez les migrations depuis une base vide":"test migrations from an empty database"}</li><li>{fr?"sauvegardez avant toute opération destructive":"back up before destructive operations"}</li></ul><p>{fr?"Au chapitre 9, nous construirons le modèle Task qui utilisera cette base pour lire, créer, modifier et supprimer les tâches avec des requêtes sûres et des transactions.":"In chapter 9, we will build the Task model that uses this database to read, create, update, and delete tasks with safe queries and transactions."}</p></section>}
export function ChapterEight({locale}:{locale:"en"|"fr"}){const fr=locale==="fr",pre=fr?"/fr":"";return <><Header locale={locale} path="/tutorial/08"/><main className="lesson-page database-lesson"><section className="lesson-hero shell"><div><p className="eyebrow"><span/>{fr?"Tutoriel MVC · Chapitre 08":"MVC tutorial · Chapter 08"}</p><h1>{fr?<>Configurer SQLite<br/><em>et les migrations.</em></>:<>Configure SQLite<br/><em>and migrations.</em></>}</h1><p>{fr?"Donnez une mémoire durable à l’application et faites évoluer son schéma sans improvisation.":"Give the application durable memory and evolve its schema without improvisation."}</p></div><aside><small>{fr?"OBJECTIF":"OUTCOME"}</small><strong>{fr?"Une base tasks reproductible":"A reproducible tasks database"}</strong><span>≈ 210 min</span></aside></section><section className="route-equation shell"><span>CONFIG</span><b>→</b><span>SQLITE</span><b>→</b><span>MIGRATIONS</span><b>→</b><strong>DATA</strong></section><div className="lesson-layout shell"><nav className="lesson-toc"><strong>{fr?"Dans ce chapitre":"In this chapter"}</strong>{lessons.map(x=><a href={`#s-${x.n}`} key={x.n}><span>{x.n}</span>{fr?x.fr:x.en}</a>)}<a href="#workshop"><span>✓</span>{fr?"Atelier":"Workshop"}</a></nav><article className="lesson-content"><Opening fr={fr}/>{lessons.map((x,i)=><section className="lesson-section route-lesson-part" id={`s-${x.n}`} key={x.n}><span className="lesson-number">{x.n}</span><h2>{fr?x.fr:x.en}</h2>{(fr?x.fp:x.ep).map((p,j)=><p className={j===0?"route-lead":undefined} key={p}>{p}</p>)}<div className="route-understand"><strong>{fr?"À retenir":"Remember"}</strong><p>{fr?x.frule:x.erule}</p></div>{i===1&&<CodeBlock>{envCode}</CodeBlock>}{i===5&&<CodeBlock>{migrationCode}</CodeBlock>}{i===6&&<CodeBlock>{`aml make:migration create_tasks_table\naml migrate\naml migrate:status`}</CodeBlock>}{i===7&&<CodeBlock>{`aml migrate:rollback\n# Verify data impact before production rollback.`}</CodeBlock>}</section>)}<section className="lesson-exercise" id="workshop"><p className="kicker">{fr?"Atelier guidé":"Guided workshop"}</p><h2>{fr?"Construisez la base de l’application.":"Build the application database."}</h2><ol>{(fr?["Initialisez .env et sélectionnez SQLite.","Placez la base dans runtime/database.","Créez la migration tasks avec ses contraintes.","Appliquez et inspectez la migration.","Testez un rollback sur des données temporaires.","Recréez la base entièrement depuis zéro.","Configurez une base distincte pour les tests.","Documentez sauvegarde et restauration de production."]:["Initialize .env and select SQLite.","Place the database under runtime/database.","Create the tasks migration and constraints.","Apply and inspect the migration.","Test rollback with temporary data.","Rebuild the entire database from zero.","Configure a separate test database.","Document production backup and restoration."]).map(x=><li key={x}>{x}</li>)}</ol></section><section className="lesson-rich-block lesson-correction"><p className="kicker">{fr?"Correction raisonnée":"Reasoned solution"}</p><h2>{fr?"Prouvez que le schéma ne dépend pas de votre ordinateur.":"Prove the schema does not depend on your computer."}</h2><p>{fr?"Supprimez uniquement la base de test, recréez-la avec les migrations, puis exécutez les vérifications. Si tout fonctionne sans modification manuelle, l’histoire du schéma est réellement reproductible.":"Delete only the test database, recreate it with migrations, then run checks. If everything works without manual changes, schema history is truly reproducible."}</p></section><Summary fr={fr}/></article></div><section className="lesson-navigation shell"><Link href={`${pre}/tutorial/07`}>← {fr?"Chapitre 07":"Chapter 07"}</Link><span>{fr?"Chapitre 09 · À venir":"Chapter 09 · Coming soon"} 🔒</span></section></main><Footer locale={locale}/></>}
