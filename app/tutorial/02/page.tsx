import type { Metadata } from "next";
import { ChapterTwo } from "../chapter-two";
export const metadata:Metadata={title:"Understand the PHPAML structure — PHPAML Tutorial",description:"Learn PHPAML MVC architecture, project folders, public assets, configuration, migrations, and the private runtime engine."};
export default function ChapterTwoPage(){return <ChapterTwo locale="en"/>}
