import type {Metadata} from "next";
import {ChapterFour} from "../../../tutorial/chapter-four";
export const metadata:Metadata={title:"Définir et organiser les routes — Tutoriel PHPAML",description:"Construisez des routes PHPAML web et API avec méthodes, paramètres, contraintes, noms, groupes, middlewares et tests."};
export default function Page(){return <ChapterFour locale="fr"/>}
