import type { Metadata } from "next";
import { ChapterThree } from "../../../tutorial/chapter-three";
export const metadata:Metadata={title:"Suivre une requête HTTP — Tutoriel PHPAML",description:"Suivez une requête PHPAML à travers public/index.php, les middlewares, le routeur, le contrôleur, le modèle, la vue et la réponse."};
export default function ChapterThreeFrenchPage(){return <ChapterThree locale="fr"/>}
